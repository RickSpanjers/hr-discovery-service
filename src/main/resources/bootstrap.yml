spring:
  profiles:
    active: dev

---

spring:
  profiles: dev
  application:
    name: spring-cloud-eureka-server
  cloud:
    config:
      uri:
      username:
      password:

---

spring:
  profiles: prod
  application:
    name: spring-cloud-eureka-server
  cloud:
    config:
      uri:
      username:
      password: